export interface ProfileInfoForm {
  id: string;
  json: string;
}

export interface ContactOfficeForm {
  id: string;
  json: string;
}

export interface ContactHomeForm {
  id: string;
  json: string;
}

export interface JobDetailForm {
  id: string;
  json: string;
}

export interface BankDetailForm {
  id: string;
  json: string;
}
